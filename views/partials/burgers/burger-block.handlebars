<li>
    {{burger_name}}

    <button class="change-status" data-id="{{id}}" data-newburger="{{devoured}}">
        {{#if devoured}}Devour it!{{else}}Hungry?{{/if}}
    </button>

    <button class="delete-burger" data-id="{{this.id}}">Delete!</button>
</li>