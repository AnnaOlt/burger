<li>
    {{burger_name}}

    <button class="change-status" data-id="{{id}}" data-status="{{devoured}}">
        {{#if devoured}}Still Hungry?{{else}}Devour it!{{/if}}
    </button>

    <button class="delete-burger" data-id="{{this.id}}">Delete!</button>
</li>